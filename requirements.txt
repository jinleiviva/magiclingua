# HY-MT 本地翻译服务 —— 运行依赖（CPU / Apple Silicon 友好）
# 安装： source venv/bin/activate && pip install -r requirements.txt
#
# 注意：
# - llama-cpp-python 默认从源码编译，需要 cmake 与 C 编译器。
# - Apple Silicon 想开启 Metal 加速（推荐），先设置环境变量再装：
#     CMAKE_ARGS="-DGGML_METAL=ON" pip install llama-cpp-python==0.3.16
# - 这里刻意不含 vllm / torch / transformers：服务端走 llama.cpp (GGUF)，
#   那几个包体量巨大且运行时用不到。
Flask==3.0.0
llama-cpp-python==0.3.16
PyMuPDF==1.27.2.2
opencv-python==4.11.0.86
numpy==2.3.5
Pillow==12.1.0
huggingface_hub==0.36.2
requests==2.32.5
babeldoc==0.6.4
